[project]
name = "data-processors"
version = "0.1.0"
description = "Workspace for MINEDUCYT data processing projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "book-02-inicial-lactantes",
    "project-03",
    "LXP-comparer",
]
